{
  "name": "routekernel-showcase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "node scripts/prepare-site.mjs && vite --host 127.0.0.1",
    "build": "node scripts/prepare-site.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test site/tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": { "vite": "8.3.0", "@playwright/test": "1.63.0" },
  "dependencies": { "@fontsource/space-grotesk": "^5.2.0", "@fontsource/ibm-plex-mono": "^5.2.0" }
}
